module.exports = {
  Egg: require('./src/egg'),
  Vent: require('./src/vent'),
  defines: require('./src/defines'),
  inherits: require('./src/inherits')}

// Object.assign||(Object.assign=bbone.assign);
  // assign: require('./lib/assign'),
// egg = new Egg
// egg.on('change', logger('egg:change'))
// egg.on('delete', logger('egg:delete'))
// egg.on('create', logger('egg:create'))

// egg.set({two: 2, five: 5})
// egg.set('one', 1);
// egg.set('two', 'II');
