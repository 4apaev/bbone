doctype html
html
  head
    meta(charset='UTF-8')
    title bbone tests
    link(rel='stylesheet', href='node_modules/mocha/mocha.css', type='text/css')
    style.
      #fixture {position: absolute; top: -9999; left: -9999;};
  body
    #mocha
    script.
      var log = console.log.bind(console);
      function call(method) {
        return Function.prototype.call.bind(method);
      }
      var slice = call([].slice)
      var own = call({}.hasOwnProperty)
      var tos = call({}.toString)
      var logger=function(msg){
        return function(){
          log(msg, arguments)
        }
      }

    script(src='node_modules/mocha/mocha.js')
    script.
      mocha.setup('bdd')
    script(src='dist/bbone.js')
    script(src='node_modules/should/should.js')
    script(src='tests/sync/base.js')
    script(src='tests/sync/request.js')
    script.
      mocha.run();